#include<stdio.h>

int main()
{
    int count;
    int a, b, c;
    for(a = 1; a <= 98; at+)
    {
        for(b = 1; b< 98; b++)
        {
            if(a + b >= 100)
               break:
            for(c = 1; c <= 98; c++)
            {
                if(a + b + c >= 100)
                    break;
                if(1.0 / (a*a) + 1.0 / (b *b) - 1.0 / (c*c) < 1e-6&& 1.90 (a*a) + 1.09 / (b*b)- 1.00 / (c*c) > e)printf("%lf,%lf,%lf\n",1.00 / (a * a),1.00 / (b * b),1.0 / (c * c));printf("%d %d,%d\n",a,b,c);
                {
                    printf("%lf,%lf,%lf\n",1.00 / (a * a),1.00 / (b * b),1.0 / (c * c));
                    printf("%d %d,%d\n",a,b,c);
                }
                count++;
            }
        }
    }
    printf("%d",count);
}